/** @type {import('next').NextConfig} */
const nextConfig = {
//     // this is to disable React's strict mode for development to avoid seeing double renders
//   reactStrictMode: false,  
};

export default nextConfig;
